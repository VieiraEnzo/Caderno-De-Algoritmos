\documentclass[9pt, a4paper, notitlepage]{extreport}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amssymb,amsmath}
\usepackage{babel}
\babelprovide[import]{portuguese}
\usepackage{comment}
\usepackage{datetime}
\usepackage[hidelinks]{hyperref}
\usepackage{graphicx}
\usepackage{multicol}
\usepackage{fancyhdr}
\usepackage[table]{xcolor}
\usepackage{enumitem}

% --- Configuração de Margens compactas ---
\usepackage[a4paper, landscape, left=0.5cm, right=0.5cm, top=1.2cm, bottom=0.8cm]{geometry}

% --- Configuração do MINTED (compacto) ---
\usepackage{minted}
\usemintedstyle{perldoc}

% Opções globais para minted - máxima compactação
\setminted{
    fontsize=\small,
    tabsize=2,
    breaklines,
    breakanywhere,
    autogobble,
    frame=none,
    framesep=0pt,
    xleftmargin=0pt,
    numbersep=2pt,
    baselinestretch=0.8
}

% Remove espaçamento extra
\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}
\setlength{\columnsep}{8pt}
\setlength{\columnseprule}{0.2pt}

% Configuração de cabeçalho e rodapé
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small UFRJ -- Meu nome é shadow. Sou aquele que deixou todo o seu passado para trás. Não faço esse contest por ninguém, não estou preso a nada}
\fancyhead[R]{\small\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\setlength{\headheight}{12pt}

% --- Numeração dos capítulos (seções) começando em 1, sem prefixo de capítulo ---
\renewcommand{\thesection}{\arabic{section}}

% --- Seções (capítulos) com título grande estilo KACTL (Huge + sublinhado) ---
\usepackage{titlesec}
\titlespacing*{\section}{0pt}{8pt plus 2pt minus 2pt}{4pt plus 1pt minus 1pt}
\titlespacing*{\subsection}{0pt}{3pt plus 1pt minus 1pt}{1pt plus 1pt minus 1pt}
\titleformat{\section}{\normalfont\Huge\bfseries}{\thesection}{0.5em}{}[{\titlerule[0.8pt]}]
\titleformat{\subsection}{\normalfont\normalsize\bfseries}{\thesubsection}{0.8em}{}

% --- Título de algoritmo: nome do arquivo, grande, sem número (estilo KACTL) ---
\newcommand{\algotitle}[1]{\par\medskip\noindent{\large\textbf{#1}}\par\smallskip}

% --- Metadados do cabeçalho: rótulo em negrito + valor, fonte pequena (estilo KACTL) ---
\newcommand{\codeheader}[2]{{\par\noindent\footnotesize{\textbf{#1:} #2\par}}}

% --- TOC: apenas capítulos (seções), sem subseções ---
\usepackage{tocloft}
\usepackage{titletoc}
\setcounter{tocdepth}{1}
\setlength{\cftbeforesecskip}{1pt}
\renewcommand{\cftsecfont}{\small\bfseries}
\renewcommand{\cftsecpagefont}{\small}
\renewcommand{\cftdotsep}{4.5}

\begin{document}

% Include cover page
\input{titlepage}

% Índice simples: somente os capítulos principais
\begin{multicols}{2}
\makeatletter\@starttoc{toc}\makeatother
\end{multicols}

\vspace{2mm}

\begin{multicols}{3}
